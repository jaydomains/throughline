# Phase E / E2 (PR #89) — executor wake-log

Executor-owned wake-log (Session 1 / EXECUTOR). One line per execution action; the commit is the wake mechanism (pure PR comments do not wake reliably). Executor-owned file — no collision with the reviewer-owned review-log path.

- 2026-05-31 — E2 implemented and DRAFT PR #89 opened at `b26407a` (off updated `main` `397d17b` = E1 squash #88; merged T-D60 present). Closes SF3-03, SF2-04, SF4-02, SF4-03; cites T-D60; **no new anchor**. SF3-03: `ChatSendResult.ai_status` ('ok'|'unavailable'|'failed') — failed call no longer masquerades as no-key. SF2-04: `RelevanceResult.classified_by_ai`; engine splits `callMade` (cost+audit) from `classifiedByAi` (classifier_used_ai). SF4-02: summariser `extractor`/`extractor_note` (mirrors dump-zone/reconcile twins), surfaced on `MdIngestedEntrySummary`. SF4-03: `MdIngestResult.skipped` typed `MdSkippedFile[]` with 6-value `MdSkipReason`. Paired tests across chat/session-start/md-ingest. Green gate local: typecheck/test (backend 533, frontend 187)/lint/build ✅. Sizing 318 ins vs band 160–230 (under +50% threshold); md-ingest peel watch not triggered.
- 2026-05-31 — round-1 fix at `c0f3e9a` (rebased onto reviewer wake-log `6c41ad9`). Finding 1 (non-blocking): `classified_by_ai` was set on parse-success even when zero tiers applied (valid-but-empty / all-mismatched response = all-medium, indistinguishable from fallback). Judged an **implementation call** (refines the boundary of a field introduced this slice, in direct service of SF2-04 — not a spec-author decision); adopted option 1 — now `applied > 0` (AI assigned a valid tier to ≥1 candidate); telemetry.model stays set so the billed call is still costed/audited. Regression: classifier-unit test asserts valid-JSON-no-tier ⇒ classified_by_ai false. Finding 2 (non-blocking, LEAVE AS-IS): failed-call extractor_note embeds raw err.message — verified to mirror the merged dump-zone/reconcile twins exactly; **flagged as a cross-cutting future-audit item** (raw upstream error string in a user-facing note across all three sites; fix uniformly in a future audit pass, not piecemeal in E2 — halt-class-8 surface-don't-expand). Both threads replied. Green gate local: typecheck/test (backend 534, frontend 187)/lint/build ✅.
- 2026-05-31 — reviewer round-1 verified all 4 disclosures + test-pinned; both findings non-blocking and addressed/acknowledged. **Status: final - approved by executor.** Verified remote HEAD via `git ls-remote` (= `c0f3e9a` before this marker). Draft→ready conversion and squash-merge remain the overseer's gate.

## Cross-cutting future-audit flag (surfaced, not fixed in E2)
- **Raw `err.message` in user-facing extractor_note** — present at `dump-zone/extractor.ts:313`, `reconcile/engine.ts:453`, and (E2) `md-ingest/summariser.ts` failed-call path. A small info-exposure + consistency question; recommend a single uniform fix in a future audit cycle rather than per-slice. (Reviewer E2 finding 2.)
