# Phase E / E3 (PR TBD) — executor wake-log

Executor-owned wake-log (Session 1 / EXECUTOR). One line per execution action; the commit is the wake mechanism. Executor-owned file.

- 2026-05-31 — E3 implemented on `claude/e3-semble-honesty` (off updated `main` `29a331f` = E2 squash #89). Closes **SF4-01** (Semble degradation honesty); cites merged **T-D60**; **no new anchor** (`SembleError` is an internal Error; `SembleStatus` is a wire enum, no T-D/C-D). Mechanism: a typed `SembleError` thrown by `client.search()` on a failed/crashed/timed-out invocation or fully-unparseable output (instead of swallowing into `[]`); `available()` returns false on a killed/timed-out probe (unconfirmed ≠ present); the service classifies `SembleError` into a tri-state `semble_status` ('available' | 'unavailable' | 'degraded') on `CodeSearchResponse` + `CodeQaResult`; frontend (ItemDetailPanel, LibraryView) discloses the degraded state instead of "no matches". `searchRepo` (best-effort enrichment) stays empty on `SembleError` but propagates real bugs. Green gate local: typecheck/test (backend 540, frontend 187)/lint/build ✅.
- 2026-05-31 — **HALT-CLASS-4 (estimate breach) SURFACED to overseer before PR action.** E3 diff ≈ 321 insertions vs plan band 70–110 (+50% ceiling = 165). Assessment: structural, not scope creep — a T-D60 honesty-disclosure slice needs the shared wire field + 3 service call-sites + frontend consumer surfaces + comprehensive paired tests (test file alone +149). Re-slicing cannot reach band without gutting coverage (backend-only ≈ 277). Same pattern as E1 (test-driven overage ruled non-halting). Awaiting overseer ruling: accept-non-halting (open draft as-is) vs re-slice.
- 2026-05-31 — re-verified the pre-existing branch commit `1522606` against current `main` `29a331f` on the fresh "proceed to E3" dispatch. Two corrections to that commit's record: (a) its body claimed `ItemDetailPanel` was updated, but it was NOT — `findCodeLinks()` still rendered "No code matches found." on a degraded search (the SF4-01 dishonesty on the item-detail surface, the slice's own stated scope). **Closed now** (disclose the degraded state via `result.semble_status === 'degraded'`) — completing stated scope, not expanding it. (b) the "+149 test / 540 backend tests" figures are confirmed accurate. Green gate re-run after the fix: typecheck/test (backend 540, frontend 187)/lint/build ✅. The estimate-breach ruling is still **OPEN** — the ItemDetailPanel completion adds ~8 lines, breach unchanged in character. Not opening the PR until the overseer rules on the breach.
