# GraphView mockups

Mockup files for graph view UI reference; uploaded separately.

These inform the Pass 1b GraphView re-plan (SPEC §7.11 / §11 DoD). Placeholder
committed so the directory exists on the branch ahead of the upload.
