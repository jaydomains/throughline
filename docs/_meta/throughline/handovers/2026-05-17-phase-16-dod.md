<!-- Template version: 1.0 -->

# Throughline — Phase 16 (Definition-of-Done walkthrough & v1 cut) Handover

**Generated:** 2026-05-17 (UTC)
**Last commit SHA:** see PR head — branch `claude/throughline-phase-16-dod-d74h2` (PR not yet merged; update to merge SHA on merge)
**Previous handover:** `docs/_meta/throughline/handovers/2026-05-17-phase-15-backup-cost-hygiene.md` (Phase 15 — backup, cost meter, settings polish, hygiene)

---

## Build State vs Spec

Phase 16 = walk every SPEC §11 bullet, close gaps, cut v1. Phases 1–15 left two open
DoD-pass items (per the Phase-15 handover) plus a latent gap surfaced during the walk.

| Spec requirement | State | Evidence | Notes |
|---|---|---|---|
| Per-feature model-override **consumed** (SPEC §9 / CODE_SPEC §14/§19) | built | `backend/src/ai/model-resolver.ts`; wired into all live AI callsites; `test/model-resolver.test.ts`, `test/chat.test.ts` (override end-to-end) | Was "panel exposes, not consumed" in Phase 15. Now resolved per call (live settings, no restart) |
| Project archive / delete functional (SPEC §11 multi-project) | built | `frontend/src/views/stubs.tsx` `ProjectsView`; `api.ts` `deleteProject`/`listProjects(includeArchived)`; `App.tsx`; `test/projectsLifecycle.test.tsx` | Backend was already functional (Phase 3); the **UI** was an unfilled Phase-3 stub — closed here |
| `rag.test.ts` embedder-agnostic (Phase-14/15 open question) | built | `backend/test/rag.test.ts:138` now `toContain(['fallback','transformers'])` | `pnpm-lock.yaml` committed (optional `@xenova/transformers` populated); test no longer assumes the dep is absent |
| Backend runs via documented single-command setup; frontend served from backend (§11) | built | `backend/package.json` (`start`/`bin` → `tsx`); smoke: `/health` 200, `/` = `index.html`, SPA route 200 via `routes/web.ts` | `node dist/index.js` could not resolve the workspace `@throughline/shared` (its `exports` → `src/*.ts`); fixed by running the entrypoints through `tsx` |
| All other §11 bullets | built | CHECKLIST §Phase 16 — 22 rows ticked with per-bullet evidence | Verified against full suite + runtime smoke; no code change needed (Phases 1–15) |

CHECKLIST §Phase 16: all 22 rows ticked. Full suite: backend **257/257**, frontend
**99/99**, `pnpm -r typecheck` clean, `pnpm build` + backend `start` smoke-verified.

---

## Last Decision Minted

> No new decisions minted. Implementation followed SPEC §9/§11, CODE_SPEC §14/§19/§21,
> T-D4. The model-override precedence (per-feature override > global `default_model`
> for Sonnet-tier features only > §14 default; Haiku-by-design features pinned) is a
> §19-vs-§14 **implementation-shape** choice, user-approved in plan, recorded in
> `ai/model-resolver.ts` + Drift Flags below — not a new anchor. The `tsx`-run
> production entrypoint is a CODE_SPEC §21-sanctioned shape choice (Drift Flag).

---

## Active Blockers

_none_

---

## Files Changed Since Last Handover

**New:**
- `packages/backend/src/ai/model-resolver.ts` — single per-feature model-resolution point.
- `packages/backend/test/model-resolver.test.ts` — precedence/alias coverage.
- `packages/frontend/test/projectsLifecycle.test.tsx` — archive/unarchive/delete coverage.
- `docs/_meta/throughline/handovers/2026-05-17-phase-16-dod.md` — this file.

**Modified:**
- `packages/backend/src/server.ts` — construct `modelFor`; thread `resolveModel` into ~13 AI factory/callsites (incl. inline tier-4 dedup).
- `packages/backend/src/{intelligence/{chat,retro,periodic-review,rag},semble/service,github/reverify,md-ingest/summariser,reconcile/engine,dump-zone/extractor,methodology/{gates,companion}/judgement,methodology/session-start/classifier}.ts` — accept `resolveModel`, resolve model per call; `rag.ts` takes the full resolver (two features).
- `packages/backend/package.json` — `start`/`bin` → `tsx`; `tsx` promoted to a dependency.
- `packages/backend/src/cli/index.ts` — shebang `#!/usr/bin/env -S tsx`.
- `packages/backend/test/{chat,rag}.test.ts` — override-consumption test; embedder-agnostic assertion.
- `packages/frontend/src/views/stubs.tsx` — `ProjectsView` archive/unarchive/delete.
- `packages/frontend/src/{api.ts,App.tsx}` — `deleteProject`, `listProjects(includeArchived)`, `onChanged` wiring.
- `packages/frontend/src/views/SettingsView.tsx` — model-override hint updated (consumption is live).
- `packages/frontend/test/fixtures/mockApi.ts` — `deleteProject`, `listProjects` archived filter.
- `CHECKLIST.md` — Phase 16 rows ticked with evidence.
- `pnpm-lock.yaml` — optional `@xenova/transformers` populated (now safe; rag test is embedder-agnostic).

**Deleted:** _none_

---

## Drift Flags

| Flag | File | What drifted | How resolved |
|---|---|---|---|
| Global-default precedence | `ai/model-resolver.ts` | §19 lists a global "default model selector"; §14 makes per-feature defaults authoritative — no precedence rule given | User-approved "Sonnet-tier only": global default moves Sonnet-tier features; Haiku-by-design (dedup, tag_suggestion, session_start) pinned unless individually overridden. Implementation-shape; documented in code header |
| Production runtime = `tsx`, not `node dist` | `backend/package.json`, `cli/index.ts` | `node dist/index.js` cannot resolve workspace `@throughline/shared` (its `exports` point at `src/*.ts` for zero-build dev/test DX) — the documented `start` was broken | Run `start`/`bin` through `tsx` (same resolution as `dev`/tests). CODE_SPEC §21 sanctions "tsup or plain tsc"; `tsc` build still runs (typecheck/artifact). Lowest-blast-radius fix for a single-user local tool; fully smoke-verified |
| ProjectsView was a stub | `frontend/src/views/stubs.tsx` | §11 "create/switch/archive/delete functional" — backend done since Phase 3, but the view was a Phase-3 placeholder with no archive/delete actions | Implemented archive/unarchive/delete (delete behind `window.confirm`, destructive-action discipline) over existing endpoints; app list kept in sync via `onChanged` |
| `rag.test.ts` + lockfile | `backend/test/rag.test.ts`, `pnpm-lock.yaml` | Phase-15 reverted the lockfile to keep CI green because the test asserted `embedder==='fallback'` | Test made embedder-agnostic; lockfile committed. The Phase-14/15 open question is now closed, not deferred again |

---

## Open Questions

- [ ] **AI callsite ↔ settings-panel key asymmetry** — the panel (`FEATURE_OVERRIDES`) exposes `tag_suggestion`, `mermaid`, `verifier_rule`, `stakeholder`, but those features have **no Anthropic callsite** (heuristic/deferred); conversely backend callsites `rag_audit`, `md_ingest`, `gates`, `periodic_review` are resolved by `modelFor` but have **no exposed override knob**. Not §11-blocking (panel-covers + consumption-where-it-exists both hold). Landing site: a model-resolver / settings-panel reconciliation slice, or a SPEC §9/§14 clarification (which features are truly AI in v1).
- [ ] **`throughline` CLI bin not discoverable via `pnpm exec`** — pnpm does not link a workspace package's own `bin` into its `node_modules/.bin`, so `pnpm --filter @throughline/backend exec throughline …` (README example) fails — pre-existing (identical with the old `dist` path), not a §11-server-bullet item. The CLI itself runs fine (`tsx src/cli/index.ts <cmd>`). Landing site: a README/dev-ergonomics fix or a root-level bin alias.
- [ ] **Multi-surface view sprawl** — Phase-13/14/15 open question persists (`GatesView`/`IntelligenceView`/`SettingsView`/`stubs.tsx` aggregates). Landing site: a UI-polish slice.
- [ ] **SESSION_START spec-author gaps** — the 7 CODE_SPEC "Questions for the spec author" + 4 `<!-- RATIONALE NEEDED -->` markers (T-D10/15/17/23) remain for the spec author; untouched this phase (none on the Phase-16 path).

---

## Recently Resolved

- **Per-feature model-override consumption** — flagged in `2026-05-17-phase-15-backup-cost-hygiene.md` Open Questions as "the remaining DoD-pass item"; resolved via `ai/model-resolver.ts` + callsite wiring.
- **`rag.test.ts` not resilient to `@xenova/transformers`** — flagged in the Phase-15 handover; resolved by an embedder-agnostic assertion + committing the lockfile (no longer deferred).
- **Project archive/delete UI** — flagged in CHECKLIST §Phase 16 ("archive/delete UI still outstanding"); implemented in `ProjectsView`.
- **Single-command setup** — `node dist/index.js` shared-resolution failure (would have failed the §11 bullet) found during the walk and fixed.

---

## Cross-Module Dependencies

**Upstream (this slice consumes):**
- `settings/service.ts` `get()` — `model_override_<feature>` / `default_model` keys.
- Every Phase 4/8/10/11/14 AI factory — now receives a `resolveModel` closure from `server.ts`.
- `projects/service.ts` + `projects/routes.ts` (DELETE, state update) — project-lifecycle UI backend.

**Downstream (consumes this slice):**
- _none_ — Phase 16 is the v1 cut; SPEC §11 is fully checkable.

---

## Reference

- Spec / decisions / build-state docs: `SPEC.md` (§9, §11), `CODE_SPEC.md` (§14, §19, §21), `DECISIONS.md` (T-D4), `CHECKLIST.md` (Phase 16), `ROADMAP.md` (Phase 16).
- PR: #24 — https://github.com/jaydomains/throughline/pull/24
